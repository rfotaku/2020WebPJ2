# Webproject2 Report  
  
##姓名及学号  
姓名 ：邓泽 学号 ：18307130177
  
##项目完成情况  
###首页  
这里的热门图片搜索收藏个数高的前8张图，而如果按了刷新按钮的话，会增加fresh参数，从而使得随机选择8张图片显示。同时如果收藏数>0的图片小于8张会按照imageid排列，即imageid小的优先展示。  
###浏览页   
分页的实现。这里要求<< 1 2 3 >>这样，左边的icon为上一页，右边的icon为下一页，页面数量大于5均截取前五页，之后的分页均与这里的要求相同。  
首先通过我们查询的语句得到了$result，通过mysqli_num_rows($result)可以得到总记录数，然后确定每一页需要展示几张图片num，两者相除并通过ceil函数向上取整来得到总页数。再通过sql语句中的 limit，来得到对应页码的记录数，而每进一页都会在url后增加page的参数，所以如果没有读到page参数，即为第一页，由此获得$page，于是我们的offset就是当前页数-1*num。前一页和后一页要控制边界，如果在第一页再往上一页即还应该为第一页，如果为最后一页再往下一页，则还是最后一页。
###搜索页    
将选择何种方式搜索的单选框以GET形式发送到当前页面进行判断，如果是刚刚加载什么都没有查，提示搜索的信息，如果是搜索了则显示结果。
###上传界面  
  采用jquery-validation插件进行表单合法性验证。
###我的照片
动态写入，所以整个页面改成php文件。
###我的收藏
基本同my_photo的逻辑，只是先从travelimagefavor中找到是否有此用户收藏的图片，如果找不到输出提示文字;如果找到了，先进行分页操作，然后逐个访问.访问时再从travelimage中利用imageid找到对应的图片，同my_photo一样的操作，进行填充。
###登陆页面  
采用jquery-validation插件进行表单合法性验证。  
###注册界面  
采用jquery-validation插件进行表单合法性验证。密码采用“必须包含大小写字母和数字的组合，不能使用特殊字符”作为强密码的要求。用户名只能由英文字母、数字和下划线组成。  
###图片详情  
把整个detail页面改成php文件，通过path来访问travelimage里面的数据，然后逐一显示。通过判断当前用户是否收藏了这张图片，如果是的话valid为true，不然为false，然后根据valid来判断收藏的状态，如果为收藏，设置按钮href跳转到取消收藏的php，如果未收藏，设置超链接a的href到收藏的php。 
  